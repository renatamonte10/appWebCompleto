
@{
    ViewBag.Title = "Contatos";

}
<div class="col-lg-8 py-4">
    <div class="info-box">
        @using (Html.BeginForm())
        {
            <div class="form-group">
                @Html.Label("Nome")
                @Html.TextBox("CustomerName", null, new { @class = "form-control", @style = "width:500px" })
            </div>
            <div class="form-group">
                @Html.Label("Comentário")
                @Html.TextArea("CustomerComment", new { @class = "textarea", @id = "summernote" })
            </div>
            <div class="box-footer">
                <button class="btn btn-primary" onclick="EnviarMensagem()" type="submit">Enviar</button>
            </div>
        }
    </div>
</div>

<address>
    One Microsoft Way<br />
    Redmond, WA 98052-6399<br />
    <abbr title="Phone">P:</abbr>
    425.555.0100
</address>

<address>
    <strong>Support:</strong>   <a href="mailto:Support@example.com">Support@example.com</a><br />
    <strong>Marketing:</strong> <a href="mailto:Marketing@example.com">Marketing@example.com</a>
</address>

@section scripts {
    <script>
        $(document).ready(function () {
            $('#summernote').summernote({
                placeholder: 'Seu comentário.',
                tabsize: 2,
                height: 100
            });
        });
    </script>
}